on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: print HEllow world
        run: echo "Hello World"

      - name: TEST TO FILE
        run: echo "Hello World2" > test.txt

  
