on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: 
      - self-hosted
      - my-vps-1

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: print HEllow world
        run: echo "Hello World"

      - name: TEST TO FILE
        run: echo "Hello World2" > test.txt
          
